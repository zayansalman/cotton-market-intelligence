# AGENTS.md

This project's agent/contributor instructions live in **[CLAUDE.md](./CLAUDE.md)** — the single source of truth for deployment rules, git workflow, tech stack, and testing.

`AGENTS.md` exists only so agent tooling that looks for this filename is pointed at the canonical guide. Do not duplicate content here; edit `CLAUDE.md` instead.
