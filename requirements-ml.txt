# Optional: Hugging Face NLP (sentiment + optional semantic relevance).
# Install after base requirements:
#   pip install -r requirements.txt -r requirements-ml.txt
#
# First run downloads model weights (~400MB+ FinBERT; ~80MB MiniLM). Set HF_HOME to cache on disk.

torch>=2.1.0
transformers>=4.36.0
sentencepiece>=0.1.99
accelerate>=0.26.0
sentence-transformers>=2.2.0
